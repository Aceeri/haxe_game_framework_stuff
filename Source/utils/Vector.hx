package utils;

// to create: {x: xValue, y: yValue}
typedef Vector = {
  var x:Int;
  var y:Int;
};

public class Vector {
  public static Normalize(v:Vector) {

  }
  // returns normalized vector from angle
  // @param a Angle in radians
  public static createVectorFromAngle(a:float) {
    
  }
}
